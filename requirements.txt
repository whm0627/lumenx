# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# AI/ML Models
dashscope>=1.20.0
pywebview>=5.0.0

# Data Validation & Processing
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
pyyaml>=6.0
PyJWT>=2.8.0

# Cloud Services
alibabacloud-videoenhan20200320>=1.0.0
alibabacloud-tea-openapi>=0.3.0
alibabacloud-tea-util>=0.3.0
oss2>=2.18.0

# Local LLM Runtime
torch>=2.3.0
transformers>=4.45.0
accelerate>=0.34.0
bitsandbytes>=0.43.0
huggingface_hub>=0.25.0
sentencepiece>=0.2.0
openai>=1.0.0
pytest>=7.4.0
pytest-asyncio>=0.23.0
llama-cpp-python>=0.3.0

# Local Image Generation (Qwen-Image via diffusers + int8 quant)
diffusers>=0.31.0
# torchao >= 0.10 requires torch >= 2.6 (uses torch.int1).
# Keep <0.10 while we're on torch 2.5.x.
torchao>=0.7,<0.10
Pillow>=10.0.0
# Required by Qwen2.5-VL video processor (used by Qwen-Image-Edit-2509
# Plus pipeline). Torchvision must match the installed torch CUDA build:
# install with --index-url https://download.pytorch.org/whl/cu121 on
# torch 2.5.x + CUDA 12.1.
torchvision>=0.20,<0.21

# Local TTS (CosyVoice-300M-SFT). Install path is non-trivial — the
# `cosyvoice` package is NOT on PyPI in a maintained form. Pick one:
#   1. Clone github.com/FunAudioLLM/CosyVoice and add to PYTHONPATH
#   2. `pip install cosyvoice` if a community wheel is available for your platform
# We list runtime support deps that ARE on PyPI here; cosyvoice itself
# is loaded lazily, so the app boots fine without it (TTS_PROVIDER=local
# users will get a clear error pointing them at the install instructions).
torchaudio>=2.5,<2.6   # match torch 2.5.x; install via the same cu121 index URL
